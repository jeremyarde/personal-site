{
  "name": "personal-website",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "export": "next export",
    "deploy": "next build && type NUL >> out/.nojekyll && next export && gh-pages -d out --dotfiles --branch master"
  },
  "dependencies": {
    "date-fns": "^2.x",
    "gh-pages": "^3.x",
    "gray-matter": "^4.x",
    "next": "^9.5.5",
    "react": "^16.14.0",
    "react-dom": "^16.14.0",
    "remark": "^12.x",
    "remark-html": "^11.x"
  }
}
